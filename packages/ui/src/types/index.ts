/** Shared prop primitives used across the design system. */
export type Size = "sm" | "md" | "lg";

export type Intent =
  | "primary"
  | "neutral"
  | "success"
  | "warning"
  | "danger"
  | "info";

/** Visual emphasis a component renders an intent with. */
export type Tone = "solid" | "soft" | "outline" | "ghost";
