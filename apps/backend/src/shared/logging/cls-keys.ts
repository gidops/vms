/** CLS context keys shared between the request middleware and event publisher. */
export const CLS_IP = 'ip';
export const CLS_USER_AGENT = 'userAgent';
/** Request locale (normalized to a backend Locale: EN | FR | AR). */
export const CLS_LOCALE = 'locale';
